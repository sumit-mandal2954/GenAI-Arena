services:
  - type: web
    name: genai-arena-backend
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
